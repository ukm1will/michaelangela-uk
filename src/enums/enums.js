const CountingItems = {
    COLUMNS: 1,
    SCORES: 2
}

const SortOrder = {
    LOWEST: 1,
    HIGHEST: 2
}

const ResultsType = {
    STABLEFORD: "Stableford",
    STROKEPLAY: "Strokeplay",
    PAIRS: "Pairs"
}

const CompetitionType = {
    STABLEFORD: 1,
    STROKEPLAY: 2
}

module.exports = {
    CountingItems,
    SortOrder,
    CompetitionType,
    ResultsType
}