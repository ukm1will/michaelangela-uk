

Add dates to columns. This would make it easier for me to check things.

Add something to show single or multiple pages.

Add something to show results week on week.


The codevolution tutorials have topics such as:

React Fundamentals and Advanced Topics
React Hooks
React Redux
React Formik
React Storybook
React Render
Practical React
React Table

not forgetting the completed tutorial:
React Testing



const handleConvertButtonClicked = () => {

        if(!userInput ||  isNaN(userInput)) {
            alert('No data present');
            return;
        }
        setIsConvertButtonClicked(true);
    }
